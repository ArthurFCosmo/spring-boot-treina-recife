
---

Agora, criaremos as classes modelo que serão gravadas no nosso BD. Elas serão as entidades que armazenaremos. Nesse projeto teremos Usuarios, Tarefas e Projetos. Aqui está o diagrama do projeto:

![[{7B9C25BB-F715-4FC5-84AF-0CDE3172B390}.png]]

Para isso, iremos à pasta main do nosso projeto, e criaremos um novo java package chamado "model"

> `src\main\java\PASTA COM O NOME DO PROJETO\` criaremos o pacote aqui.


## A classe Usuario

Dentro do pacote model, começaremos criando a classe de Usuario, como falei antes.

Vamos definir os atributos:

![[{62E4C3A7-E165-4F09-9675-041000AB962F}.png]]
Veja que ainda não criamos a Classe de Enum do atributo Status.

### Enum

Enum é um tipo de Classe no Java para predefinir valores. Por exemplo, queremos que atributos da classe StatusUsuario possam receber apenas 3 valores: ATIVO, INATIVO e BLOQUEADO. Então criamos o ENUM dentro de um pacote de enums que deve ser criado na pasta do projeto da seguinte forma:

![[{C52EEF8E-07F8-4241-B4A3-96B0D8544D1A}.png]]

![[{0D1D44F1-3858-470F-8FD8-952AB54F567C}.png]]

Depois, para utilizá-lo nos atributos de Usuario, basta importar.

### Anotações Spring

Agora vamos fazer o Srping trabalhar por nós. Com as anotações, podemos indicar pra ele como usar essa nossa classe no banco de dados.

Primeiro, no nosso atributo de ID, colocaremos duas anotações:

```Java
@Id 
@GeneratedValue(strategy = GenerationType.IDENTITY)
private Long id;
```

> **@Id** - Anotação para fazer o spring reconhecer o atributo a seguir como chave primária

> **@GeneratedValue(strategy = GenerationType.IDENTITY)** - Anotação para fazer o spring boot fazer o atributo a seguir ser auto gerado

- **Note que não é necessário ; no fim de anotações**

Na nossa classe, anotaremos o seguinte:

```Java
@Entity(name = "TB_USUARIOS")
public class Usuario {

	...

}
```

> **@Entity(name = "TB_USUARIOS")** - Anotação para fazer o Srping reconhecer essa classe como modelo de entidade e criar uma tabela dela no BD.

Também anotaremos na nossa classe:

> **@Data** - Anotação do lombok para criar getters e setters implícitos, executados em tempo de compilação
 
> **@NoArgsConstructor** - Anotação do lombok para criar construtor vazio (Exigido pelo Spring)

> **@AllArgsConstructor** - Anotação do lombok para criar construtor com os parâmetros.

